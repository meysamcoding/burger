<div class="container">

    <h1> Burger!</h1>

</div>

<div class="row container">
    <div class="col-lg-4" id="devoured">
        <h3 id="heaven">I am eating:</h3>

        <ul>
            {{#each burger}}
            {{#unless this.devoured}}
            <li>
                <form action="/burger/{{this.id}}?_method=POST" method="POST">
                    <button type="submit"
                         >
                        {{this.burger_name}}</button>
                </form>
            </li>
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="col-lg-4" id="devoured">
        <h3 id="heaven">I ate:</h3>

        <ul>
            {{#each burger}}
            {{#if this.devoured}}
            <li>
                <p>{{this.burger_name}} </p>
            </li>
            {{/if}}
            {{/each}}
        </ul>

    </div>
</div>
<div class="container" id="order">
    <form action="/burger" method="POST" class="center-align">
    <input type="text" id="name"   name="burger_name" placeholder="what do you want?">
    <button type="submit"
        > add order
        </button>
</form>
</div>
